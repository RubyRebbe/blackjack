<%= simple_form_for(@user_game) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :game %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
