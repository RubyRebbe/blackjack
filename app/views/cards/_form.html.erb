<%= simple_form_for(@card) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :rank %>
    <%= f.input :suit %>
    <%= f.association :user_game %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
