<%= simple_form_for(@game) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :win %>
    <%= f.input :lose %>
    <%= f.input :tie %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
